# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets (Piano API, GSC service account, etc.)
.env
.env.local
gsc-service-account*.json
*-service-account*.json

# CSV bruts déposés par l'agent IA — seuls les JSON dérivés sont versionnés
raw-csv/

# Sorties Playwright codegen (peuvent contenir des creds en clair, à sanitiser avant tout commit)
codegen-*.js

# Screenshots de debug produits par les scripts Playwright en cas d'erreur
data/raw/**/_error-*.png

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code session data
.claude
